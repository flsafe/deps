(ns deps.core
  (:require [com.stuartsierra.component :as component]
            [deps.database :as db])
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
